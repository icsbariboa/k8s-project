properties([githubProjectProperty(displayName: '', projectUrlStr: 'https://github.com/icsbariboa/k8s-project/'), pipelineTriggers([upstream('k8s-CI')])])
node("prod") {
        stage("Helm Repository") {
            bat "helm repo add k8s-repo https://github.com/icsbariboa/k8s-project"
            bat "helm repo update"
        }
        stage("Run Producer") {
            bat "helm upgrade --install k8s-repo/Producer-0.1.0"
        }
        stage("Run Consumer") {
            bat "helm upgrade --install k8s-repo/Consumer-0.1.0"
        }
}
