apiVersion: v1
kind: Pod
metadata:
  name: producer
  labels:
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
  restartPolicy: Never
